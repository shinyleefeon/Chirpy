# Chirpy
HTTP webserver built with boot.dev course

after running go build this program can be run with go run /chirpy
it runs on a sql database and webserver specified in the .env file

There are several api endpoints to pull, sort, authenticate and post "chirps" which are basically tweets
